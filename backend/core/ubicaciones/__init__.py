"""Ubicaciones domain services — static-map capture + Pillow composition.

Subpaquete extraído de ``backend.handlers.ubicaciones`` (simplification-016).
El módulo ``backend.handlers.ubicaciones`` sigue siendo la superficie pública
para tests (monkeypatch) y para ``backend.handlers.__init__``; aquí vive la
implementación dividida por responsabilidad.
"""
